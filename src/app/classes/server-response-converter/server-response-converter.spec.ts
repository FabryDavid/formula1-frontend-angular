import {ServerResponseConverter} from './server-response-converter';

describe('ServerResponseConverter', () => {
  it('should create an instance', () => {
    expect(new ServerResponseConverter()).toBeTruthy();
  });
});
