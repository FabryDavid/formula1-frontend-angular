import {FlagHostDirective} from './flag-host.directive';

describe('FlagHostDirective', () => {
  it('should create an instance', () => {
    const directive = new FlagHostDirective();
    expect(directive).toBeTruthy();
  });
});
