import {SectorColorPipe} from './sector-color.pipe';

describe('SectorColorPipe', () => {
  it('create an instance', () => {
    const pipe = new SectorColorPipe();
    expect(pipe).toBeTruthy();
  });
});
