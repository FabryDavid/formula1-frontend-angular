import { WeekendDatesPipe } from './weekend-dates.pipe';

describe('WeekendDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekendDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
